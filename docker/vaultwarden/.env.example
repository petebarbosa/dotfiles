# DOMAIN: Full HTTPS URL for Vaultwarden (used in emails/links)
DOMAIN=https://subdomain.yourdomain.com

# VW_HOSTNAME: Hostname for Traefik routing (no https://)
VW_HOSTNAME=subdomain.yourdomain.com
