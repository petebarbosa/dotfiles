# TRAEFIK_AUTH_USERS: Basic auth for dashboard (htpasswd format, $ doubled)
# Generate: docker run --rm httpd:alpine htpasswd -nbB username password | sed -e 's/\$/\$\$/g'
TRAEFIK_AUTH_USERS=username:hashed_password
