# CF_TUNNEL_ID: UUID of your Cloudflare tunnel (from <UUID>.json filename)
CF_TUNNEL_ID=your-tunnel-uuid

# CF_DOMAIN: Root domain managed in Cloudflare DNS
CF_DOMAIN=yourdomain.com

# CF_SSH_SUBDOMAIN: Random slug for SSH access (e.g., t3rm, c7x9)
CF_SSH_SUBDOMAIN=your_random_slug
